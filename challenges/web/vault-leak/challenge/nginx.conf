server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Add a custom header to all responses. This is the clue.
        add_header X-Old-Version /index.html.bak always;
        try_files $uri $uri/ =404;
    }
}